# scripts/.env.deploy にコピーして使う（gitignore 済み）。
# CloudFormation スタック名（任意の識別子）
STACK_NAME=lodestar
# デプロイ先リージョン（S3 バケットの作成先。CloudFront はグローバル）
AWS_REGION=ap-northeast-1
# 使う AWS プロファイル（必要な場合のみ。未設定なら既定の資格情報を使用）
# AWS_PROFILE=default
